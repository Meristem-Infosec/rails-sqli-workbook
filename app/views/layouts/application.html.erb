<!DOCTYPE html>
<html>
  <head>
    <title>SqlInjector</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
<header>
  <%= form_with url: searches_show_path(turbo_frame: "query_builder"), method: :get do |f| %>
    <%= f.label(:query_builder, "What query would you like to try?") %>
    </p><%= f.select "query_action", @queries %>
    </p><%= f.text_area 'sql_string' %>
    </p><%= f.submit("Submit SQL Query", :string => nil) %>
  <% end %>

<turbo-frame id="query_builder" target="_top"></turbo-frame>
    </header>
    <main><%= yield %></main>
  </body>
</html>
